export module rem.mat;

export namespace rem {

template <typename T, unsigned L>
class mat {

    // std::array<T,L> data;
public:
    /*
        template<T ... Args,
            typename = std::enable_if_t<sizeof...(Args) == L>>
        vec(Args... args){

        }*/
};

}  // namespace rem
